export interface ContactLink {
  id: string;
  icon: React.ReactNode;
  name: string;
  href: string;
}

export interface SocialLink {
  id: string;
  icon: React.ReactNode;
  name: string;
  href: string;
}
